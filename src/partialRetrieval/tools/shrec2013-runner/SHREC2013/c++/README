To compile, just type:

 > make

To execute:

> ./evaluator2 target.cla query.cla file.res -bin value [-model | -class | -all] value [partiality]

where:
 - target.cla: classification file of target models
 - query.cla: classification file of query models
 - file.res: Distance matrix
 - value: Number of samples in precision recall values (tipically 10)
 - if option model or class is specified, the next "value" means the model index or class index, respectively
 - partiality: 0 or 1 (not including or including partiality factor.)
